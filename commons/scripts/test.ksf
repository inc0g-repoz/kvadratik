// Commenting
on test(e) {
    var d = e.call(2) ^ 4;
    e.log.info(d);

    var i = 0;
    while (i < 10) {
        i = i + 1;
        e.log.info(i);
    }

    if (1 == 1 && !false) {
        e.log.info("true");
    }
}
