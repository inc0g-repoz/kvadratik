// Commenting
on test(e) {
    var d = e.call(2) ^ 4;
    e.log.info(d);

    var i = 0;
    while (i < 10) {
        i = i + 1;
        e.log.info(i);
    }

    if (1 == 2) e.log.info("true");
    else e.log.info("false");
}
