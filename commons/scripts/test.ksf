// Commenting
on test(e) {
    var testVar = e;
    var two = testVar.self().call(2);
    e.log.info(two);

    var str = "bca";
    if (true) {
        var l = e.log;
        l.info("test 2");
    }
}
